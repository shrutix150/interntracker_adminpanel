rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary development rule for the admin panel.
    // Tighten this before production by requiring admin auth.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
